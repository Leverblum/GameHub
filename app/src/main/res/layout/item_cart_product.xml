<?xml version="1.0" encoding="utf-8"?>
<!-- Cambiado a un ConstraintLayout para más flexibilidad -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:layout_marginBottom="8dp"
    android:background="@drawable/edittext_background">

    <ImageView
        android:id="@+id/ivCartProductImage"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_gamepad"
        android:contentDescription="@string/product_image" />

    <TextView
        android:id="@+id/tvCartProductName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:fontFamily="@font/inter_18pt_medium"
        android:textColor="@color/text_primary"
        android:textSize="16sp"
        android:maxLines="2"
        android:ellipsize="end"
        app:layout_constraintStart_toEndOf="@id/ivCartProductImage"
        app:layout_constraintTop_toTopOf="@id/ivCartProductImage"
        app:layout_constraintEnd_toStartOf="@+id/btnRemoveItem"
        tools:text="Título del producto muy largo que ocupa dos líneas" />

    <TextView
        android:id="@+id/tvCartProductPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/inter_18pt_bold"
        android:textColor="@color/color_primary"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@id/ivCartProductImage"
        app:layout_constraintStart_toStartOf="@id/tvCartProductName"
        tools:text="$59.99" />

    <!-- NUEVO: Botón para eliminar el ítem -->
    <ImageButton
        android:id="@+id/btnRemoveItem"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_delete"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:contentDescription="Eliminar producto" />

    <LinearLayout
        android:id="@+id/quantityStepper"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="@id/ivCartProductImage"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageButton
            android:id="@+id/btnMinus"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/btn_circle_outline"
            android:src="@drawable/ic_minus"
            android:contentDescription="Restar uno" />

        <TextView
            android:id="@+id/tvQuantity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:fontFamily="@font/inter_18pt_bold"
            android:textColor="@color/text_primary"
            android:textSize="16sp"
            tools:text="1" />

        <ImageButton
            android:id="@+id/btnPlus"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/btn_circle_outline"
            android:src="@drawable/ic_add"
            android:contentDescription="Añadir uno" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
