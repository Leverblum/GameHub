<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_item_payment"
    android:gravity="center_vertical"
    android:layout_marginBottom="8dp"
    android:padding="12dp">

    <!-- Ícono de la tarjeta (Visa, Mastercard, etc.) -->
    <ImageView
        android:id="@+id/img_payment_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/icono_metodo_pago"
        tools:src="@drawable/ic_payment" />

    <!-- Nombre de la tarjeta -->
    <TextView
        android:id="@+id/tv_payment_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@id/img_payment_icon"
        android:layout_toStartOf="@id/btn_delete_payment"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:textSize="16sp"
        android:textStyle="bold"
        tools:text="Visa **** 1234" />

    <ImageView
        android:id="@+id/btn_delete_payment"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:contentDescription="@string/eliminar_metodo_pago"
        android:focusable="true"
        android:src="@drawable/ic_delete" />

</RelativeLayout>
