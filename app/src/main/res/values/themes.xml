<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal de la aplicación (para MainActivity, etc.) -->
    <style name="Theme.GameHub" parent="Theme.Material3.Light.NoActionBar">
        <!-- Colores primarios del tema -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/color_secondary</item>

        <!-- Colores de fondo -->
        <item name="android:colorBackground">@color/color_background</item>
        <item name="colorSurface">@color/color_surface</item>

        <!-- Colores de texto -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <!-- Fuente global por defecto -->
        <item name="android:fontFamily">@font/inter</item>
    </style>

    <!-- Tema para la SplashActivity. Es igual pero se nombra por claridad. -->
    <style name="Theme.GameHub.Splash" parent="Theme.GameHub" />

</resources>
