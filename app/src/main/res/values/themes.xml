<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.GameHub" parent="Theme.Material3.Light.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="android:colorBackground">@color/color_background</item>

        <!-- Fuente global -->
        <item name="android:fontFamily">@font/inter</item>
    </style>

    <!-- Estilos de títulos -->
    <style name="TextAppearance.GameHub.H1" parent="@android:style/TextAppearance.Material.Headline">
        <item name="android:textSize">28sp</item>
        <item name="android:fontFamily">@font/orbitron</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextAppearance.GameHub.H2" parent="@android:style/TextAppearance.Material.Title">
        <item name="android:textSize">22sp</item>
        <item name="android:fontFamily">@font/orbitron</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextAppearance.GameHub.Body" parent="@android:style/TextAppearance.Material.Body1">
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/inter</item>
        <item name="android:textColor">@color/text_secondary</item>
    </style>
</resources>
